# Shared

Código verdaderamente transversal y reutilizable entre bounded contexts: componentes genéricos, utilidades, infraestructura común y recursos compartidos.

Evitar usar `shared` como contenedor de lógica de negocio específica.
